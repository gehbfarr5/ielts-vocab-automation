# 同步提醒与弹窗清单

2026-09-17。Mac Anki 25.09.4源码和本机sync.pyc确认：aqt.sync同步NO_CHANGES完成分支调用tooltip(tr.sync_collection_complete())。AnkiConnect的sync调用最终进入mw.onSync，因此后台同步会触发Anki原生界面。后台Python及当前“雅思截屏 控制中心”没有自定义成功通知动作。

## 调整

本地插件deployment/anki_quiet_sync仅包装aqt.sync模块自己的tooltip，按当前界面语言精确匹配sync_collection_complete；命中时不显示，其他消息透传。没有全局关闭系统通知，没有跳过同步、屏蔽错误或修改AnkiConnect。安装位置Anki2/addons21/ielts_quiet_sync；必须重启Anki加载。卸载该目录并重启可回退。未来Mac Server部署时一并安装，不能仅复制Python后台代码。

这是窄范围本地适配，复用Anki现有翻译标识和tooltip入口。[官方源码](https://github.com/ankitects/anki/blob/25.09.4/qt/aqt/sync.py)直接定位；未增加第三方插件依赖，未运行测试或主动触发同步。升级Anki后若入口变化需重新检查。

## 当前流程可能出现的界面

| 类别 | 触发条件 | 处理 |
|---|---|---|
| 同步完成短暂提示 | Mac Anki正常完成同步，包括无变更 | 本地插件屏蔽；重启生效 |
| 同步进度/媒体同步状态 | Anki正在同步或传输媒体 | 保留，属于进度而非成功通知 |
| AnkiWeb登录/认证错误 | 未登录、凭据失效 | 保留 |
| 网络/服务/同步异常 | 同步出现错误 | 保留；后台异常另写日志，不保证都会弹窗 |
| 上传/下载选择 | 需要全量同步或两端冲突 | 保留；不自动选择 |
| 空集合上传/下载确认 | 可能用空集合替换数据 | 保留 |
| AnkiWeb服务端消息 | 同步响应包含server_message | 保留 |
| 快捷指令系统授权/运行错误 | 初次权限、文件访问或动作失败 | 系统决定；本机同步的快捷指令没有自定义提醒动作 |

清单范围为本项目链路，不是所有Anki插件或系统的全部弹窗。当前快捷指令只有关闭控制中心、延时、截图、复制剪贴板、自动存文件五个动作；未设置显示通知、显示提醒、菜单或询问保存位置。照片另存已去除。iPad/iPhone实际副本状态未在本轮重新读取。

正常收件、分析、写卡、pending/额度等待在Python后台没有弹窗；持久故障主动推送仍未实现。关闭成功提示不会等同于关闭这些故障诊断。

部署状态：插件已安装，因Mac Anki窗口控制连续超时，本轮未重启，待用户退出再打开后生效。未宣称运行中已静默。
