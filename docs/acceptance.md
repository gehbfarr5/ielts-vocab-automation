# v0.1 验收与剩余工作

日期：2026-09-16。结论：**开发预览可发布；正式 Level D 未完成验收。**

## 已核验证据

- Python 3.12.14，uv 0.12.5，锁定依赖；本地 Ruff/格式检查通过。
- 27项自动测试通过：并发末额度、跨日库存、恢复日锁、Context独立额度、首次学习事件、重复ID/别名冲突、截断文件包、来源/学术证据拒绝、HTML转义、写成功但响应丢失、静默更新失败与人工编辑冲突。
- 已有 Anki 程序实际版本25.09.4。官方上游 AnkiConnect v6 在独立测试资料库加载成功，身份、模型、卡和历史接口返回正常。
- 实际创建1条Note并增加1个Context义项；读回2张卡，预算为1核心/2卡/1Context；旧核心释义保持不变。未操作个人词库。
- Apple Vision 已在自行生成的英文三色测试图本地执行；原图与OCR结果仅保存在私有测试目录，未发布图片。
- loopback收件HTTP服务已运行；未认证请求401，认证状态请求成功。每分钟worker已注册。

自动化、原生API读回与移动端视觉验收分别记录。本轮没有iPad/iPhone真实UI或复习同步证据，不用Mac API结果代替移动端PASS。

## 当前阻塞与缺口

| 项目 | 状态 | 下一步 |
|---|---|---|
| 公开GitHub版本管理 | 本版本发布后以远端提交与CI结果为准 | 验证远端可见性及检查结果 |
| 云图像分析 | BLOCKED：数据处理选择尚未确认，实际调用未验收 | 确认云/本地范围后接入并测试 |
| Shortcut共享输入 | INTERRUPTED：已创建入口草稿，首次CLI测试超时后停止 | 核对原生首次运行、文件权限和iCloud实际落点；做iPad分享测试 |
| 正式Anki范围 | BLOCKED：资料库/旧Note映射/未学库存未确认 | 只读盘点、备份、映射后少量灰度 |
| 颜色/词义质量 | 未验收：只有合成OCR测试，无独立真实标注集 | ≥30张/100标记的校准与验收划分 |
| 自动每日政策 | 尚未实现；当前day接口要求明确输入事实 | 接通复习用时、逾期、抽测、学习日与库存 |
| Level4完整证据 | 部分实现：通用词频已接；CEFR/AWL/用户历史融合待接 | 许可、数据覆盖、义项匹配与证据测试 |
| 跨端同步 | 未验收 | Mac/AnkiWeb/移动端内容与首次学习回传 |
| 固定相册自动入口 | 尚未实现 | 仅在确定其为必需后实现受支持照片访问 |
| 常态维护 | 部分实现 | 日志轮转、保留策略、受管字段回滚工具 |

## 发布限制

默认不自动改正式Anki、不启用云分析，校准未通过的ACCEPT转为DEFER。不要把更改配置布尔值当作通过验收。预算账本能防止自身超发，但不能拦截用户在多个未同步设备上的额外学习。

下一阶段目标是用真实但最小化的输入完成受控闭环，而不是增加界面或扩展卡片种类。上述缺口解决前，不发布“完整自动同步已完成”的结论。
